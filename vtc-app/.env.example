# Example environment file (NO REAL SECRETS).
# Copy to .env and fill with your real values. Do NOT commit .env.

# Public base URL (production). Example: https://your-app.onrender.com
APP_URL=

# --- Supabase (multi-tenant) ---
# URL du projet Supabase
SUPABASE_URL=
# Clé publique côté navigateur (utilisée pour l'auth + requêtes RLS côté dashboard)
# Nom recommandé: SUPABASE_PUBLISHABLE_KEY
SUPABASE_PUBLISHABLE_KEY=
# Compat historique (optionnel): SUPABASE_ANON_KEY=

# Clé serveur (optionnelle) — utilisée uniquement pour opérations admin / API sans session.
# Nom recommandé: SUPABASE_SECRET_KEY
SUPABASE_SECRET_KEY=
# Compat historique (optionnel): SUPABASE_SERVICE_ROLE_KEY=

# --- SMTP (required for email notifications) ---
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=

# --- Booking email (required) ---
BOOKING_EMAIL_FROM=

# NOTE: en mode multi-tenant, l'adresse TO vient de tenant_settings.booking_email_to.
# BOOKING_EMAIL_TO n'est pas utilisée par /create-booking (pas de fallback global).
BOOKING_EMAIL_TO=

# --- Slack (optional) ---
# Key used to encrypt Slack webhooks in DB (AES-256-GCM).
# Provide 32 bytes in base64 or 64 hex chars.
ENCRYPTION_KEY=
# Compat historique (optionnel): CONFIG_ENCRYPTION_KEY=

# DEV-only fallback (explicit):
# - Never used in production
# - Only used when ALLOW_DEFAULT_SLACK_FALLBACK=true
ALLOW_DEFAULT_SLACK_FALLBACK=false
DEFAULT_SLACK_WEBHOOK_URL=

# (Legacy) fallback source (still supported in dev when gate enabled)
SLACK_WEBHOOK_URL=

# --- AI Assistant (optional, server-side only) ---
# IMPORTANT: Never expose this key in the theme/widget.
# Enable/disable the endpoint:
AI_ASSISTANT_ENABLED=false
# OpenAI API key used by the server proxy route (/apps/vtc/api/ai-assistant)
OPENAI_API_KEY=
# Optional model override
OPENAI_MODEL=gpt-4o-mini
